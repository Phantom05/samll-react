// export const apiAddress = `https://my-json-server.typicode.com/kakaopay-fe/resources/words`;

export const NODE_ENV = process.env.NODE_ENV;
export const NODE_DEV = NODE_ENV === 'development';
export const NODE_PROD = NODE_ENV === 'production';

export const address = {
  api: 'https://my-json-server.typicode.com',
  github: `https://github.com/Phantom05`,
};
